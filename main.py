from algorithms.lda.lda import get_lda
from tools import get_clusters


a = []
a.append((1, "ali"))
a.append((2, "alis"))
print(a[0][0])
# clusters = get_clusters(get_lda(False), './QA-samples-reduced.xlsx')
# top_n_docs = 8
# for x in clusters:
#     x.print(top_n_docs)
#     print("-------------\n")
