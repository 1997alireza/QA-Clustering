from sklearn.metrics.pairwise import euclidean_distances
from nltk.metrics import edit_distance


def preform_test(clusters, train_records, test_records):
    # threshold = 20
    # for i in range(len(test_questions)):
    #     min_dist = 10000000
    #     min_dist_index = 1000000
    #     for j in range(len(train_questions)):
    #         tmp = euclidean_distances(test_questions[i], train_questions[j])
    #         if (tmp < min_dist):

    print("s")


def draw_histogram():
    print("")
